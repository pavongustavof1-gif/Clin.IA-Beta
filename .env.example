# AssemblyAI
ASSEMBLYAI_API_KEY=your_assemblyai_api_key_here

# Google Gemini
GEMINI_API_KEY=your_gemini_api_key_here

# Supabase
# JWT verification uses Supabase's JWKS endpoint (ES256), not a shared
# secret — SUPABASE_JWT_SECRET is obsolete and intentionally not listed.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_KEY=your_supabase_service_role_key_here
SUPABASE_ANON_KEY=your_supabase_publishable_anon_key_here

# ICD-11 API
ICD_CLIENT_ID=your_icd_client_id_here
ICD_CLIENT_SECRET=your_icd_client_secret_here

# Resend (email delivery)
RESEND_API_KEY=your_resend_api_key_here

# Flask
FLASK_SECRET_KEY=change_this_to_a_random_secret_in_production

# Logging — INFO/WARNING/ERROR/DEBUG. Defaults to INFO if unset. DEBUG is
# an explicit opt-in only — never leave it set in production, and note
# that no log statement in this codebase should ever emit raw transcript
# or patient content regardless of level (Stage H1 fix #11).
LOG_LEVEL=INFO
